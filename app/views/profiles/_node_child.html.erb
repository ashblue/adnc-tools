<div data-node="child" class="panel panel-default" id="<%= child.id %>">
    <div class="panel-heading">
        Child
        <%= link_to '<span class="glyphicon glyphicon-minus-sign"></span> Delete'.html_safe,
                    child,
                    :remote => true,
                    :method => :delete,
                    :confirm => 'Permanently delete this node?',
                    :class => 'btn btn-default btn-xs' %>
        <span class="status"></span>
    </div>
    <div class="panel-body">
        <div class="row">
            <div class="col-md-8">
                <%= form_for child, html: { :class => 'form-horizontal' } do |f| %>

                    <div class="form-group">
                        <%= f.label :xpath, :class => 'col-sm-2 control-label' %>
                        <div class="col-sm-10">
                            <%= f.text_field :xpath, :class => 'form-control' %>
                        </div>
                    </div>

                    <div class="form-group">
                        <%= f.label :xpath_alt, :class => 'col-sm-2 control-label' %>
                        <div class="col-sm-10">
                            <%= f.text_field :xpath_alt, :class => 'form-control' %>
                        </div>
                    </div>

                    <div class="form-group">
                        <%= f.label :json_name, :class => 'col-sm-2 control-label' %>
                        <div class="col-sm-10">
                            <%= f.text_field :json_name, :class => 'form-control' %>
                        </div>
                    </div>

                    <div class="form-group">
                        <%= f.label :helper, :class => 'col-sm-2 control-label' %>
                        <div class="col-sm-10">
                            <%= f.select :helper, XmlHelpers.methods(false).reverse,
                                         {},
                                         { :class => 'form-control' } %>
                        </div>
                    </div>

                    <div class="form-group attr <%= 'hide' if !child.helper == 'connectors' %>">
                        <%= f.label :return_attr, 'Return attribute?', :class => 'col-sm-2 control-label' %>
                        <div class="col-sm-10">
                            <%= f.text_field :return_attr,
                                         :placeholder => 'Defaults to inner_text',
                                         :class => 'form-control' %>
                        </div>
                    </div>

                    <div class="form-group">
                        <%= f.label :enforce_type, :class => 'col-sm-2 control-label' %>
                        <div class="col-sm-10">
                            <%= f.select :enforce_type, ['array', 'int', 'boolean'],
                                     { :include_blank => 'none' },
                                     { :class => 'form-control' } %>
                        </div>
                    </div>

                <% end %>
            </div>

            <div class="col-md-4">
                <h4>Sample</h4>
                <pre class="sample"><%= child.xpath_sample %></pre>
            </div>
        </div>
    </div>
</div>