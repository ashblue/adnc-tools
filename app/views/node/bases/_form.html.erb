<%= form_for(@node_basis) do |f| %>
  <% if @node_basis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@node_basis.errors.count, "error") %> prohibited this node_basis from being saved:</h2>

      <ul>
      <% @node_basis.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :xcode %><br>
    <%= f.text_field :xcode %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
